LibreCat experimental CERIF export from a JSON input file

 * cpanm Template::Plugin::Digest::MD5 
 * Change the proj_dir in bin/json2serif
 * Uncomment the CERIF files you need
 * Run: bin/json2serif -d INSTALL_DIRECTORY export.json CERIF.zip
